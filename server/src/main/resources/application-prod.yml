server:
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:mysql://${MYSQLHOST}:${MYSQLPORT:3306}/${MYSQLDATABASE}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ${MYSQLUSER}
    password: ${MYSQLPASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      # SSE 스트림이 connection 을 길게 잡는 특성 + saveIfAbsent 동기 쓰기 → 풀 고갈 위험.
      # Railway MySQL 동시 연결 한도 안에서 보수적으로 확대 (5 → 10).
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 8000       # ms — 풀 대기 한도 (기본 30s 너무 김)
      validation-timeout: 3000
      leak-detection-threshold: 30000 # 30s 초과 연결 보유 시 경고 로그

  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: Asia/Seoul

kakao:
  rest-api-key: ${KAKAO_REST_API_KEY}
  client-secret: ${KAKAO_CLIENT_SECRET}
  redirect-uri: ${KAKAO_REDIRECT_URI}

claude:
  api-key: ${CLAUDE_API_KEY}
  model: ${CLAUDE_MODEL:claude-sonnet-4-6}

admin:
  secret: ${ADMIN_SECRET:}

# JWT 인증 — JWT_SECRET 환경변수 필수 (32자 이상)
jwt:
  secret: ${JWT_SECRET:dev-jwt-secret-change-me-min-32-chars-long-please}
  ttl-minutes: ${JWT_TTL_MINUTES:43200}
  sse-ttl-seconds: ${JWT_SSE_TTL_SECONDS:60}
  legacy-fallback: ${JWT_LEGACY_FALLBACK:true}

clova:
  tts:
    client-id: ${CLOVA_CLIENT_ID:none}
    client-secret: ${CLOVA_CLIENT_SECRET:none}
    speaker: vdain
    cache-dir: ./tts-cache
